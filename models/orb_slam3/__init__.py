"""
ORB-SLAM3 model package.

Submodules mirror the mapanything_pipeline/ layout:
    api      — sequence resolution, rgb.txt, settings YAML, active list, trajectory load
    metrics  — displacement diagnostic, trajectory stats, GPS alignment, ATE
    viz      — keyframe trajectory, GPS overlay, displacement chart
    tracking — OrbSlam3Run (MLflow context manager)
"""
